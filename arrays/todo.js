// Create an array with 5 todos
// Print You have x todos
// Print first and second to last item -> Todo:

const todos = ["make breakfast", "yoga", "shower", "programme", "walk"];

console.log(`You have ${todos.length} todos`);
console.log(
  `First todo: ${todos[0]}, Second to last todo: ${todos[todos.length - 2]}`
);
